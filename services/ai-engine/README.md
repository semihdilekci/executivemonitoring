# AI engine service

Canonical Python paketi: **`services/ai_engine/`**

Deploy/Lambda path adı `services/ai-engine/` ile uyumludur; Python modül adında tire kullanılamadığı için kaynak kod `services/ai_engine/` altındadır.

Detay: `services/ai_engine/README.md`
